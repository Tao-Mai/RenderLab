# RenderLab Agent Rules

本文件适用于整个仓库。

## 禁止兼容代码

- 本项目按当前设计直接演进，不保留向后兼容代码。
- 用户修改数据结构、接口、类名、字段名、资源格式或场景格式后，新设计立即成为唯一标准。
- 必须删除被替代的旧类、旧字段、旧文件、旧接口、旧重载、旧别名和旧 include。
- 禁止添加旧格式回退解析、双格式读写、兼容分支、适配层、弃用入口或“先尝试新格式，再尝试旧格式”的逻辑。
- 禁止为了兼容旧资产而给新格式中的必填字段设置旧格式默认行为。缺少必填字段时应明确报错。
- 修改格式或接口时，必须在同一次改动中同步更新所有调用方、shader、场景文件、资产文件、构建配置和相关测试。
- 完成修改后，必须搜索被移除的旧符号和旧字段，确认仓库中没有残留引用，并执行构建验证。

只有用户明确要求兼容某个旧版本时才允许实现迁移；迁移逻辑应作为一次性的独立工具存在，不得混入正常运行路径。

## 测试目录

- 需要测试时，Agent 可以直接在 `test/` 目录编写和运行测试，无需额外确认。
- Agent 拥有 `test/` 目录的完整写入和维护权限，可自行新增、修改、移动或删除其中的测试代码、夹具、测试资源和构建配置。
- 测试代码及其专用资源统一放在 `test/`；按改动范围运行相关测试。
