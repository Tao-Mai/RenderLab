---
description: OpenGL 4.5+ 使用 Direct State Access（DSA），禁止传统 bind 式资源操作
globs: "**/*.{cpp,h,hpp}"
alwaysApply: false
---

# OpenGL：Direct State Access（DSA）

本项目 OpenGL **4.5 Core**。新建/修改 GPU 资源代码时，默认用 **DSA（对象名直接操作）**，不要用 bind + 改全局状态的老 API。

## 必须遵守

| 资源 | ✅ DSA | ❌ 避免 |
|------|--------|--------|
| Buffer | `glCreateBuffers`, `glNamedBufferData`, `glNamedBufferSubData` | `glGenBuffers`, `glBindBuffer`, `glBufferData` |
| Texture | `glCreateTextures`, `glTextureStorage*`, `glTextureParameteri` | `glGenTextures`, `glBindTexture`, `glTexImage2D`, `glTexParameteri` |
| Renderbuffer | `glCreateRenderbuffers`, `glNamedRenderbufferStorage` | `glGenRenderbuffers`, `glBindRenderbuffer` |
| FBO 附件 | `glCreateFramebuffers`, `glNamedFramebufferTexture`, `glNamedFramebufferRenderbuffer`, `glNamedFramebufferDrawBuffers` | `glGenFramebuffers`, `glFramebufferTexture2D` |
| VAO 布局 | `glCreateVertexArrays`, `glVertexArrayVertexBuffer`, `glVertexArrayAttribFormat`, `glVertexArrayElementBuffer` | `glGenVertexArrays` + `glBindVertexArray` 配顶点属性 |
| 采样器绑定 | `glBindTextureUnit(unit, tex)` | `glActiveTexture` + `glBindTexture` |

参考：`base/gfx/mesh_gpu.h`、`base/editor/id_picker.cpp` 初始化路径。

## 允许的例外（运行时切换，不是资源创建）

- **绘制**：`glBindVertexArray(vao)` + `glDrawElements`（无 DSA draw API）
- **切换渲染目标**：pass 间可用 `glBindFramebuffer` / `glBindFramebuffer(GL_READ_FRAMEBUFFER, …)`；附件配置仍用 `glNamedFramebuffer*`
- **全局状态**：`glViewport`, `glEnable`, `glClear`, `glUseProgram`（shader program 无 DSA）

## 示例

```cpp
// ❌ 绑定式
GLuint tex;
glGenTextures(1, &tex);
glBindTexture(GL_TEXTURE_2D, tex);
glTexStorage2D(GL_TEXTURE_2D, 1, GL_RGBA8, w, h);

// ✅ DSA
GLuint tex;
glCreateTextures(GL_TEXTURE_2D, 1, &tex);
glTextureStorage2D(tex, 1, GL_RGBA8, w, h);
glBindTextureUnit(0, tex);  // 仅绑到纹理单元，供 shader 采样
```

```cpp
// ❌ FBO 附件
glBindFramebuffer(GL_FRAMEBUFFER, fbo);
glFramebufferTexture2D(GL_FRAMEBUFFER, GL_COLOR_ATTACHMENT0, GL_TEXTURE_2D, tex, 0);

// ✅ DSA
glNamedFramebufferTexture(fbo, GL_COLOR_ATTACHMENT0, tex, 0);
```

## 注意

- 优先 `glCreate*` 而非 `glGen*`（与 DSA 配套）
- 读回像素：`glBindFramebuffer(GL_READ_FRAMEBUFFER, fbo)` + `glReadPixels` 可接受；能用 `glGetTextureSubImage` 等 DSA 读 API 时优先
- 不要在新代码里引入 GLAD 未暴露的扩展；保持与 `main.cpp` 中 4.5 core 一致
