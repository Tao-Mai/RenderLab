重构本项目

资产系统：
UUID，需要一个64位的UUID生成器，所有资产都用，通过资产类型来区分不同资产

assets不再放obj这种模型文件，而是放json文件用来描述资产，按资产类型创建子文件夹

assetmanager 启用时扫描assets所有json文件并加载其源数据进内存

用entt 做静态反射以及运行时修改，资产的json序列化和反序列化一步完成

built-in资产不需要UUID也不用放入assets

scene作为一个资产类型放入assets用json描述，app.yaml依然在config里

不同的系统，将它们的设计详细写入notes/docs中，分文件写

现在暂时做资产系统